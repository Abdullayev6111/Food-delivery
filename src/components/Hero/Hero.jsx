import styles from "./Hero.module.css";
function Hero() {
    return (
        <section className="container">
            <div className={styles.hero}></div>
        </section>
    );
}

export default Hero;
